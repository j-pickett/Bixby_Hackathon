action (SubmitSpot) {
  description (An action that takes a required Spot as an input)
  type (Constructor)
  collect {
    input (category) {
      type (Category)
      min (Required) max (One)
    }
    input (spotName) {
      type(SpotName)
      min (Required) max(One)
    }
    input (description) {
      type (description)
      min (Required) max (One)
    }
  }
  output (Spot)
}
