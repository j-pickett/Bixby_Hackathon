endpoints {

  action-endpoints {
    action-endpoint (SubmitSpot) {
      accepted-inputs (spotName, email, comments, isSigningUp)
      local-endpoint (submitSpot.js)
    }
    action-endpoint (FindPersons) {
      accepted-inputs ()
      local-endpoint (findPersons.js)
    }
  }
}
